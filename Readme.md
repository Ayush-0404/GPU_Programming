# CUDA Batch Image Processor

GPU-accelerated batch image processing using custom CUDA kernels.
Converts RGB images to grayscale and optionally applies a box blur.

## Requirements

- NVIDIA GPU with CUDA support
- CUDA Toolkit (nvcc)
- Linux/macOS

## Building

```bash
make build
